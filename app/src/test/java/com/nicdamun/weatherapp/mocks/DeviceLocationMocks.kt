package com.nicdamun.weatherapp.mocks

import com.nicdamun.common.dtos.DeviceLocationDTO
import com.nicdamun.common.models.DeviceLocationModel

val normalDeviceLocationDTO = DeviceLocationDTO(
    latitude = 1.0,
    longitude = 1.0
)

val normalDeviceLocationModel = DeviceLocationModel(
    latitude = 1.0,
    longitude = 1.0
)